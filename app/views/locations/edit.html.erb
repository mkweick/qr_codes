<% @page_title = 'Edit Location' %>

<%= render 'shared/ajax_loader_js', form_id: 'location-form' %>
<%= render 'shared/errors', obj: @location %>

<div class="row">
  <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
    <div class="well">
      <div class="text-center">
        <h2>Edit Location</h2>
        <div class="font-20"><%= @location.name %></div>
      </div>
      <hr />
      <%= render 'form', btn_text: 'Update' %>
    </div>
  </div>
</div>