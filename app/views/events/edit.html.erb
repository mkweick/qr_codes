<% @page_title = 'Edit Event Name' %>

<%= render 'shared/errors', obj: @event %>

<div class="row"> 
  <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
    <div class="well text-center">
      <h2>Update Event Name</h2>
      <span class="font-20"><%= @event.name %></span>
      <hr class="top-10" />
      <%= render 'event_form', btn_text: "Update", show_cancel_btn: true %>
    </div>
  </div>
</div>