<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body { font-size: 20px; }
  </style>
</head>
<body>
  <%= "#{@first_name if @first_name} #{@last_name if @last_name}" %>
  from
  <%= "#{@account_name if @account_name} has arrived at the #{@event.name}!" %>
</body>
</html>