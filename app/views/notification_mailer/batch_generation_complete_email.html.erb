<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body { font-size: 20px; }
  </style>
</head>
<body>
  <strong>QR Codes and Excel Export:</strong>
  <p>
    <a href="<%= event_url(@event) %>">
      <% if @batch.location %>
        <%= "#{@event.name} - #{@batch.location} - Batch #{@batch.number}" %>
      <% else %>
        <%= "#{@event.name} - Batch #{@batch.number}" %>
      <% end %>
    </a>
  </p>
</body>
</html>